# コミットメッセージ

コミットメッセージに
`Co-Authored-By: Claude Opus 4.7 noreply@anthropic.com`
のような協働メッセージは不要

# PR description

PR descriptionにはサマリと、大きめの粒度のやったこと、コードベースからは判断できない設計思想や意思決定などを記載する
長くなりすぎると読みにくいので簡潔に
原因のロジックなど、長くなってしまう場合はレイアウトを工夫する

## ドキュメント・Claudeファイルの更新

ドキュメントやClaudeファイルのいわゆるmarkdownファイルの更新時は、ユーザーのレビュー負荷を鑑みて重要な要素を削らずできるだけ簡潔に or 見やすい形で最小限の更新とすること。

## 修正ルール

- PRでレビューがすでにされているものについては、承認を得ない限り追加commitで修正対応する
- 未レビューのものはamend・fixupしてもよい
- 修正後、botへの返信のみ許可、人間への返信はいかなる時も許可しない
- コードを修正した場合は意図が読める範囲でworking treeに残置する

## 基本の実装サイクル方針
ユーザーが途中で意図して指示した場合は以下のとおりではない。
- コードに変更を加えた場合は意図が読める範囲でworking treeに残置する
- 承認が得られるまでユーザーに確認・修正
- ユーザーが確認を明確に承認した段階で一時commitを打つ(正式なcommitではない)
- 既に一時commitが存在する場合はamendしていく
- ある程度意味のある正式レビュー可能な単位になったら正式コミットとしてコミットメッセージを整理する
- 次の実装に入る=>繰り返し

## コメント
コード内のコメントはできるだけ最小限に保つ。
- **長々と経緯や検証履歴を書いてはいけない**
- **意味のないスタイル用改行はいれない**
- コードベースから自明なコメントは避ける
  - 例）`if (user == null) return`に対して「ユーザーが存在しない場合は抜ける」
- 実装自体が長く概要の把握が困難な場合は適宜簡潔に説明しても良い
- 重要な設計判断や、一見不要そうなコードだが不具合の改善である場合は経緯や意思判断を数行以内で簡潔に添える
- 実装した関数や機能の仕様元の都合は書かない(コメント更新漏れを避けるため)
